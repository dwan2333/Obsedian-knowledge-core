```python
'''Most common error types:
1, SynataxError (you broken the grammer rules of python. Mostlikely some thing happened with a missing coloumn or :)

2, NameError (You called out a function or varible that is not defined yet)

3, TypeError (**:** You tried to perform an operation on a data type that doesn't support it. For example like adding a num to a string)

4, IndexError (You tried to access an item in a list (or sequence) at a position that doesn't exist.)

5, KeyError (similar to `IndexError`, but for Dictionaries. You tried to look up a key that isn't in the dictionary. Common Cause: Assuming a key exists without checking.)

6, ValueError (The _type_ of the data is correct, but the _content_ (value) is inappropriate for the operation.for example int(Hello))

7, AttributeError (you tried an attribute thath the module dose not have for example list.update())

8, ZeroDivisionError (you broke the rule of the unverisal math principle where 0 are not divisble)'''

# Try (try the this code first)
# Except (if the try code dose not work excute this one)
# else (if try succeeded without errors I willl run this code next)
# finally (no matter what happened I will run this code)

```