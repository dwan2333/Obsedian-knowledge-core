```python fold:index(x)
# get the index of the first occurrence of x
# work on str ([[Build in string functions]]) and tuple
lst = ['a', 'b', 'c']
print(lst.index('b'))
```
```python fold:count(x)
# Count how many time x appears
# work on str ([[Build in string function]]) and tuple as well
lst = [1, 2, 2, 3]
print(lst.count(2))
```
