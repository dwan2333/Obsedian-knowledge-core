```python fold:sort(x,key,reverse=False)
# sort the list
# if key is given sort according to the key (e.g len, abs)
# if reverse = True sort in decending order
lst = [3, 1, 2]
lst.sort()
print(lst)
```
```python fold:reverse()
# reverse the list in place
lst = [1, 2, 3]
lst.reverse()
print(lst)
```
