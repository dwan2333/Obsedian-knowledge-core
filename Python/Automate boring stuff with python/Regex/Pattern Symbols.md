```python fold:pattern_symbols
#. any character except new line
# \d Digit(0-9), \D nonDigit
# \w word character (a-z, A-Z, 0,9) \W non word character
# \s white space characters (\n\,t, etc) \S non - whitespace (\n,\t,etc) 
# \b word booundary \B non word boundary
```