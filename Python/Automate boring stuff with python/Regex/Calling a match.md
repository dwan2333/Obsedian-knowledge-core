```python fold:calling_a_match
# match.group() calls entire match
# match.group(number) calls the match of your number 
# match.groups() returns all group as tuple
# match.groupdict() returns dictionary of name groups
# match.start(group) return starting index of group number
# match.end(group) return ending index of group number
# match.span(group) return start and end of group index 

```