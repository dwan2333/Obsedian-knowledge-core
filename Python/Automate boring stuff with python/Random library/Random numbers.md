
```python fold:random.random()
import random
print(random.random()) #include 0 exclude 1
```
```python fold:random.uniform(a,b)
import random
print(random.uniform(1,2)) #inclusive for both a and b, return float
```
```python fold:random.int(a,b)
import random
print(random.randint(1,2)) #inclusive for both a and b, return int
```
```python fold:random.randrange(start,stop,step)
import random
print(random.randrange(1,8,2)) #inclusive only for a, return based on step 
```
